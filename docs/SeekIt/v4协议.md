# v4 协议图表

## 1. 协议类型体系

```mermaid
flowchart TB
    subgraph 消息类型
        direction TB
        MB[_MessageBase<br/>基础类]

        MB --> EP[ErrorPayload<br/>code / message / hint<br/>retryable / docs_url / details]
        MB --> PI[PeerInfo<br/>name / role / version]

        MB --> Init[InitializeMessage<br/>id / protocol_version / peer<br/>handlers / capabilities / metadata]
        MB --> Out[InitializeOutput<br/>peer / protocol_version<br/>capabilities / metadata]

        MB --> Inv[InvokeMessage<br/>id / capability / input<br/>stream / caller_plugin_id]
        MB --> RM[ResultMessage<br/>id / kind / success<br/>output / error]
        MB --> EM[EventMessage<br/>id / phase<br/>data / output / error]
        MB --> CM[CancelMessage<br/>id / reason]

        Init -.->|包含| PI
        Init -.->|包含| HD
        Out -.->|包含| PI
        RM -.->|包含| EP
        EM -.->|包含| EP
    end

    subgraph 描述符类型
        direction TB
        DB[_DescriptorBase<br/>基础类]

        DB --> Perm[Permissions<br/>require_admin / required_role / level]
        DB --> CT[CommandTrigger<br/>command / aliases / platforms]
        DB --> MT[MessageTrigger<br/>regex / keywords / platforms]
        DB --> ET[EventTrigger<br/>event_type]
        DB --> ST[ScheduleTrigger<br/>cron / interval_seconds]

        DB --> HD[HandlerDescriptor<br/>id / trigger / kind<br/>priority / permissions / filters]
        DB --> CD[CapabilityDescriptor<br/>name / description<br/>input_schema / output_schema<br/>supports_stream / cancelable]

        HD --> Perm
        HD --> Trig
    end

    Trig{Trigger<br/>联合类型}
    Trig --> CT
    Trig --> MT
    Trig --> ET
    Trig --> ST

    style MB fill:#f5f5f5
    style DB fill:#f5f5f5
    style Trig fill:#fff2cc
    style 消息类型 fill:#e3f2fd
    style 描述符类型 fill:#fff3e0
```

## 2. 完整交互流程

```mermaid
sequenceDiagram
    participant W as Worker (Plugin)
    participant S as Supervisor (Core)

    Note over W,S: ═══════════ 握手阶段 ═══════════

    W->>S: InitializeMessage
    Note right of W: {id, protocol_version, peer,<br/>handlers, capabilities, metadata}

    alt 握手成功
        S->>W: ResultMessage
        Note left of S: {kind: "initialize_result",<br/>success: true, output: {...}}
        Note over W,S: ✅ 连接就绪
    else 握手失败
        S->>W: ResultMessage
        Note left of S: {success: false,<br/>error: protocol_version_mismatch}
        Note over W,S: ❌ 连接关闭
    end

    Note over W,S: ═══════════ 普通调用 ═══════════

    S->>W: InvokeMessage (stream=false)
    Note right of S: {id, capability, input}

    alt 成功
        W->>S: ResultMessage
        Note left of W: {id, success: true, output}
    else 失败
        W->>S: ResultMessage
        Note left of W: {id, success: false, error}
    end

    Note over W,S: ═══════════ 流式调用 ═══════════

    S->>W: InvokeMessage (stream=true)
    Note right of S: {id, capability, input, stream: true}

    W->>S: EventMessage (phase: started)

    loop 增量数据
        W->>S: EventMessage (phase: delta)
        Note left of W: {id, phase: "delta", data}
    end

    alt 正常完成
        W->>S: EventMessage (phase: completed)
        Note left of W: {id, phase: "completed", output}
    else 流式错误
        W->>S: EventMessage (phase: failed)
        Note left of W: {id, phase: "failed", error}
    else 用户取消
        S->>W: CancelMessage
        Note right of S: {id, reason: "user_cancelled"}
        W->>S: ResultMessage
        Note left of W: {id, success: false, error: cancelled}
    end
```

## 3. 系统架构与状态

```mermaid
flowchart TB
    subgraph Supervisor进程
        direction TB
        SR[SupervisorRuntime]
        SP[Peer]
        CR[CapabilityRouter]
        HD[HandlerDispatcher]

        SR --> SP
        SR --> CR
        CR --> SP
        SP --> HD
    end

    subgraph Transport层
        direction LR
        T1[StdioTransport<br/>子进程管道]
        T2[WebSocketTransport<br/>网络连接]
    end

    subgraph Worker进程
        direction TB
        WR[WorkerSession]
        WP[Peer]
        GR[GroupWorkerRuntime]
        PR[PluginWorkerRuntime]

        WR --> WP
        WR --> GR
        GR --> PR
    end

    SP <-->|"协议消息"| T1
    T1 <-->|"协议消息"| WP
    SP <-->|"协议消息"| T2
    T2 <-->|"协议消息"| WP

    style Supervisor进程 fill:#e3f2fd
    style Worker进程 fill:#e8f5e9
    style Transport层 fill:#fff3e0

    subgraph 能力命名空间
        direction TB
        Root[能力命名空间]

        Root --> LLM[llm.*]
        Root --> MEM[memory.*]
        Root --> DB[db.*]
        Root --> PLAT[platform.*]
        Root --> PERM[permission.*]
        Root --> SYS[system.*]
        Root --> HND[handler.* ❌]
        Root --> INT[internal.* ❌]

        LLM --> L1[llm.chat]
        LLM --> L2[llm.stream_chat]
        MEM --> M1[memory.search]
        DB --> D1[db.get / db.set]
        PLAT --> P1[platform.send]
        PERM --> PE1[permission.check]

        style HND fill:#ffcccc
        style INT fill:#ffcccc
    end

    subgraph 调用状态机
        direction LR
        [*] --> Sent: InvokeMessage

        Sent --> WaitResult: stream=false
        Sent --> WaitEvents: stream=true

        WaitResult --> Done: ResultMessage
        WaitEvents --> Started: started
        Started --> Delta: delta
        Delta --> Delta: delta
        Delta --> Done: completed
        Delta --> Fail: failed

        Delta --> Cancelled: CancelMessage
        Cancelled --> Done

        Done --> [*]
        Fail --> [*]
    end
```
